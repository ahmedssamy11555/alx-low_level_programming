#include "main.h"

/**
 * _isalpha - checks if entery is alphbet
 *
 * Description:  ** checks if entery is alphbet**
 *@c : integar parameter
 * Return: 1 if alphabet and 0 if not
 */

int _isalpha(int c)
{
if (c >= 'A' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}

